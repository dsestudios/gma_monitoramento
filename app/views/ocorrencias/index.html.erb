<%= admin_bloco do %>

<p><%= link_to("Novo", new_ocorrencia_path) %></p>

<table id="tabela_listagem" width="100%" border="0" cellspacing="6">
  <tr>
    <th>Categoria</th>
    <th width="30%">Ação</th>
  </tr>

<% @ocorrencias.each do |ocorrencia| %>
  <tr>
    <td><%= ocorrencia.grupo %></td>
    <td><%= links_to_operacao(ocorrencia) %></td>
  </tr>
<% end %>
</table>

<%= link_to_back %>

<% end %>