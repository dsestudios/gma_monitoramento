<%= simple_form_for(@visor) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :nome %>
    <%= f.input :numero, :as => :integer %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
